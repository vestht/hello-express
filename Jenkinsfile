pipeline {

    agent any

    stages {

        stage("build") {
            steps {
                sh "echo 'hello'"
                sh "pwd"
                sh "ls -all"
            }
        }

    }

}